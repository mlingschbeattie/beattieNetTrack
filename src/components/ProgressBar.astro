---
const { value = 0 } = Astro.props;
const safeValue = Math.min(100, Math.max(0, value));
---

<div class="progress" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow={safeValue}>
  <div class="progress__bar" style={`width: ${safeValue}%`}></div>
</div>
