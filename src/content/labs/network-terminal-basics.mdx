---
title: Network Terminal Basics Lab
description: Practice essential network diagnostics in a safe terminal simulation.
slug: network-terminal-basics
track: network-engineer
moduleId: net.troubleshooting.tools
module: net.troubleshooting.tools
order: 99
difficulty: Beginner
estMinutes: 20
tags: ["terminal", "network", "diagnostics"]
labPath: /labs/network-terminal-basics/index.html
checkLabel: Check Commands
submitLabel: Submit Lab
hints:
  - Run help first to confirm available simulator commands.
  - Use pwd before and after cd to verify your location.
  - Use cat to read files needed for validation.
checklist:
  - Completed required terminal commands
  - Captured expected command output
  - Ran Check before Submit
steps:
  - id: help
    title: Show help
    prompt: Run the built-in help command to list available simulator commands.
    hint: The simulator supports a `help` command.
    validator:
      type: exact
      value: help
    successMessage: 'Help displayed'
  - id: ipconfig
    title: Show network configuration
    prompt: Run the command that prints full IP configuration for the host.
    hint: On Windows try `ipconfig /all`, on POSIX use `ifconfig` or `ip a`.
    validator:
      type: oneOf
      values: ['ipconfig /all', 'ifconfig', 'ip a']
    successMessage: 'IP configuration captured'
  - id: nslookup
    title: Resolve DNS
    prompt: Run the command to resolve `beattietech.edu` using DNS lookup.
    hint: Try `nslookup beattietech.edu` or `dig +short beattietech.edu`.
    validator:
      type: oneOf
      values: ['nslookup beattietech.edu', 'dig +short beattietech.edu']
    successMessage: 'DNS lookup succeeded'
---

Use the terminal simulator to practice real network troubleshooting commands.

## Goals

- Run command-line diagnostics and inspect host network state.
- Identify IP configuration and DNS details.
- Complete checks before submitting.

## Required commands

1. `help`
2. `ipconfig /all`
3. `nslookup beattietech.edu`

<Callout type="tip">
Use **Ctrl+Enter** in the workspace to trigger Submit quickly.
</Callout>
