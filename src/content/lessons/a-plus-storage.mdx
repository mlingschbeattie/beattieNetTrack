---
title: Storage Devices - A+ Study Guide
slug: a-plus-storage
legacyUrl: /a-plus-storage.html
difficulty: medium
estimatedMinutes: 15
---

import LegacyHtml from '../../components/LegacyHtml.astro';

<LegacyHtml html={`<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Storage Devices - A+ Study Guide</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="site.css" />
    
  </head>
  <body>
    <a href="#main-content" class="skip-link">Skip to content</a>
    
    <header class="site-header">
      <div class="header-container">
        <div class="logo">
          <a href="/lessons/index">
            <span class="logo-bracket">[</span>
            <span class="logo-text">BEATTIE</span>
            <span class="logo-bracket">]</span>
            <span class="logo-subtitle">TECH</span>
          </a>
        </div>
        
        <button class="mobile-menu-toggle" aria-label="Toggle menu">
          <span></span>
          <span></span>
          <span></span>
        </button>
        
        <nav class="main-nav nav-links">
          <ul>
            <li><a href="/lessons/index">🏠 Home</a></li>
            <li><a href="/lessons/study-guides" class="active">📖 Study Guides</a></li>
            <li><a href="/lessons/learning-tracks">🎯 Learning Tracks</a></li>
            <li><a href="/lessons/resources">🔬 Study Hub</a></li>
            <li><a href="/lessons/cheat-sheets">⚡ Cheat Sheets</a></li>
          </ul>
        </nav>
      </div>
    </header>
    
    <div class="scanlines"></div>
    
    <main id="main-content" class="container">
      <nav class="breadcrumbs">
        <a href="/lessons/index">Home</a>
        <span class="separator">/</span>
        <a href="/lessons/study-guides">Study Guides</a>
        <span class="separator">/</span>
        <a href="/lessons/a-plus-guides">A+ Guides</a>
        <span class="separator">/</span>
        <span class="current">Storage Devices</span>
      </nav>
      
      <h1>Storage Devices</h1>
      <div class="subtitle">CompTIA A+ - HDD vs SSD, partitioning, file systems, and RAID</div>

      <div class="section">
        <h2>ELI5: Storage vs Memory</h2>
        <div class="info-box">
          RAM is like your desk—fast to work with but clears every time you leave. Storage is like your filing cabinet—slower to access but holds everything permanently.
        </div>
      </div>

      <div class="section">
        <h2>Practical Exercises</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">Drive Type ID</div><div class="description">Identify HDD, SATA SSD, and NVMe by appearance</div></div>
          <div class="command-item"><div class="command">Partition Plan</div><div class="description">Create a partition layout for OS + data + recovery</div></div>
          <div class="command-item"><div class="command">File System Match</div><div class="description">Match NTFS/FAT32/ext4/APFS to use cases</div></div>
          <div class="command-item"><div class="command">RAID Scenarios</div><div class="description">Pick RAID level for speed vs redundancy</div></div>
        </div>
      </div>

      <div class="section">
        <h2>HDD vs SSD Comparison</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">HDD (Mechanical)</div><div class="description">Spinning platters, slow but cheap, good for bulk</div></div>
          <div class="command-item"><div class="command">SSD (Flash)</div><div class="description">No moving parts, fast, expensive, standard now</div></div>
          <div class="command-item"><div class="command">NVMe (M.2)</div><div class="description">Ultra-fast PCIe drive, fastest storage type</div></div>
          <div class="command-item"><div class="command">Connection Types</div><div class="description">SATA, SAS, NVMe, PCIe</div></div>
          <div class="command-item"><div class="command">Speed (Read/Write)</div><div class="description">HDD: 80–200 MB/s; SSD: 500+ MB/s; NVMe: 3500+ MB/s</div></div>
          <div class="command-item"><div class="command">Lifespan</div><div class="description">HDD: 3–5 years; SSD: 5–10 years (write-cycle limited)</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Partitioning & File Systems</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">MBR (Master Boot Record)</div><div class="description">Legacy, max 2TB, 4 primary partitions</div></div>
          <div class="command-item"><div class="command">GPT (GUID Partition Table)</div><div class="description">Modern, supports 9ZB, unlimited partitions</div></div>
          <div class="command-item"><div class="command">NTFS</div><div class="description">Windows standard, supports 16EB files</div></div>
          <div class="command-item"><div class="command">FAT32</div><div class="description">Older, universal, 4GB file size limit</div></div>
          <div class="command-item"><div class="command">ext3/ext4</div><div class="description">Linux file systems, journaling for stability</div></div>
          <div class="command-item"><div class="command">APFS</div><div class="description">Apple's modern file system (macOS, iOS)</div></div>
        </div>
      </div>

      <div class="section">
        <h2>RAID Arrays</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">RAID 0 (Striping)</div><div class="description">Speed boost, no redundancy (risky)</div></div>
          <div class="command-item"><div class="command">RAID 1 (Mirroring)</div><div class="description">Exact copy on 2 drives, redundancy</div></div>
          <div class="command-item"><div class="command">RAID 5 (Striping + Parity)</div><div class="description">Speed + redundancy, min 3 drives</div></div>
          <div class="command-item"><div class="command">RAID 10 (1+0)</div><div class="description">Mirror of stripes, best performance</div></div>
          <div class="command-item"><div class="command">RAID 6</div><div class="description">Dual parity, survive 2 drive failures</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Storage Interfaces</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">SATA III</div><div class="description">Up to 6 Gbps, common for HDD/SSD</div></div>
          <div class="command-item"><div class="command">NVMe (PCIe)</div><div class="description">Low‑latency, very high throughput</div></div>
          <div class="command-item"><div class="command">USB Storage</div><div class="description">External drives and flash media</div></div>
          <div class="command-item"><div class="command">eSATA</div><div class="description">External SATA (legacy)</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Backup Types & Imaging</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">Full Backup</div><div class="description">Copies everything each time</div></div>
          <div class="command-item"><div class="command">Incremental</div><div class="description">Changes since last backup (fastest)</div></div>
          <div class="command-item"><div class="command">Differential</div><div class="description">Changes since last full backup</div></div>
          <div class="command-item"><div class="command">System Image</div><div class="description">Snapshot of entire OS drive</div></div>
          <div class="command-item"><div class="command">File-Level Backup</div><div class="description">Selected folders and documents</div></div>
          <div class="command-item"><div class="command">3‑2‑1 Rule</div><div class="description">3 copies, 2 media, 1 offsite</div></div>
        </div>
      </div>

      <div class="section">
        <h2>NAS & Cloud Storage</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">NAS</div><div class="description">Network‑attached storage for shared files</div></div>
          <div class="command-item"><div class="command">SMB / NFS</div><div class="description">Common network file protocols</div></div>
          <div class="command-item"><div class="command">Cloud Sync</div><div class="description">OneDrive, Google Drive, iCloud</div></div>
          <div class="command-item"><div class="command">Versioning</div><div class="description">Restore previous file versions</div></div>
          <div class="command-item"><div class="command">Access Controls</div><div class="description">Permissions on shared folders</div></div>
          <div class="command-item"><div class="command">RAID ≠ Backup</div><div class="description">Redundancy is not a backup plan</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Drive Health & Maintenance</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">SMART</div><div class="description">Drive self‑monitoring indicators</div></div>
          <div class="command-item"><div class="command">Defragmentation</div><div class="description">HDD only; never for SSD</div></div>
          <div class="command-item"><div class="command">TRIM</div><div class="description">SSD optimization to maintain speed</div></div>
          <div class="command-item"><div class="command">Bad Sectors</div><div class="description">Signs of failing HDD</div></div>
          <div class="command-item"><div class="command">Drive Imaging</div><div class="description">Clone a disk for recovery</div></div>
          <div class="command-item"><div class="command">Monitoring Tools</div><div class="description">Check drive health and temperature</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Exam Facts</h2>
        <div class="example-block">
          <div class="example-title">Key Points to Remember</div>
          <div class="example-code">• GPT + UEFI is modern standard
• NTFS supports permissions (NTFS-style ACLs)
• FAT32 still used for USB drives for compatibility
• SSD trim command keeps performance high
• RAID is not a backup (one bad batch can kill all)
• Hot-swappable drives need hot-swap capable drive bays
• Monitor SMART data for drive health predictions
• SSDs have write-cycle limits (MTBF rating)</div>
        </div>
      <div class="section">
        <h2>Interactive Quiz</h2>
        <div id="progress-badge" style="display: none; text-align: center; margin-bottom: 15px;">
          <span style="background: linear-gradient(135deg, #00ff88, #00d4ff); color: #0a0e27; padding: 8px 20px; border-radius: 20px; font-weight: 700; font-size: 0.95em;">
            ✓ COMPLETED
          </span>
        </div>
        <div id="previous-score" style="display: none; text-align: center; margin-bottom: 15px; color: #00d4ff;">
          Previous Best: <strong id="best-score-display">0</strong> | Attempts: <strong id="attempts-display">0</strong>
        </div>
        <div class="info-box" style="margin-bottom: 20px;">
          <strong>Dynamic Quiz:</strong> Questions randomized from an expanded pool. Refresh for new questions!
        </div>
        <div class="quiz-mode-controls">
          <label>Review length:</label>
          <button data-review="5min">5 min</button>
          <button data-review="15min">15 min</button>
          <button data-review="30min">30 min</button>
        </div>
        <div id="review-mode-note" class="review-mode-note"></div>
        <div id="quiz-container">
          {/* Questions generated dynamically */}
        </div>
      </div>

      <div class="section">
        <h2>Mini-Lab: RAID Configuration Simulator</h2>
        <div class="lab-container">
          <div class="info-box">
            <strong>Objective:</strong> Calculate RAID capacity and understand redundancy
          </div>
          <div class="lab-challenge">
            <div class="raid-config">
              <div class="psu-input-row">
                <label>RAID Level:</label>
                <select id="raid-level">
                  <option value="0">RAID 0 (Striping - No redundancy)</option>
                  <option value="1">RAID 1 (Mirroring)</option>
                  <option value="5">RAID 5 (Striping with parity)</option>
                  <option value="10">RAID 10 (Mirror + Stripe)</option>
                </select>
              </div>
              <div class="psu-input-row">
                <label>Number of Drives:</label>
                <input type="number" id="drive-count" value="4" min="2" max="8">
              </div>
              <div class="psu-input-row">
                <label>Drive Capacity (GB):</label>
                <input type="number" id="drive-capacity" value="1000" min="100" step="100">
              </div>
            </div>
            <div class="lab-input-group">
              <button id="raid-calculate">Calculate RAID Capacity</button>
            </div>
            <div id="raid-result" class="psu-result"></div>
          </div>
        </div>
      </div>

      </div>
    </div>

    

    <script type="module">
      import { aPlusStorageQuestions } from "./data/a-plus-storage-questions.js";
      import { getProgress, updateProgress } from "./js/progressStore.js";

      const TRACK_ID = "pc-professional";
      const MODULE_ID = "a-plus-storage";

      const REVIEW_PRESETS = {
        "5min": 6,
        "15min": 15,
        "30min": 30
      };

      const DEFAULT_PRESET = "5min";

      let currentScore = 0;
      let questionsAnswered = 0;
      let totalQuestions = 0;
      let activePreset = DEFAULT_PRESET;

      function shuffleArray(array) {
        const shuffled = [...array];
        for (let i = shuffled.length - 1; i > 0; i--) {
          const j = Math.floor(Math.random() * (i + 1));
          [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
        }
        return shuffled;
      }

      function buildReviewSet(presetKey) {
        const numQuestions = REVIEW_PRESETS[presetKey] || REVIEW_PRESETS[DEFAULT_PRESET];

        const easy = aPlusStorageQuestions.filter(q => q.difficulty === "easy");
        const med = aPlusStorageQuestions.filter(q => q.difficulty === "medium");
        const hard = aPlusStorageQuestions.filter(q => q.difficulty === "hard");

        let pool;
        if (presetKey === "5min") {
          pool = [...easy, ...med];
        } else if (presetKey === "15min") {
          pool = [...easy, ...med, ...hard];
        } else {
          pool = [...med, ...hard];
        }

        if (pool.length === 0) {
          pool = [...aPlusStorageQuestions];
        }

        const shuffled = shuffleArray(pool);
        return shuffled.slice(0, Math.min(numQuestions, shuffled.length));
      }

      function setActiveButton(presetKey) {
        document.querySelectorAll('.quiz-mode-controls button').forEach(btn => {
          btn.classList.toggle('active', btn.dataset.review === presetKey);
        });
      }

      function updateReviewModeNote(presetKey) {
        const note = document.getElementById('review-mode-note');
        const labelMap = {
          "5min": "5-minute",
          "15min": "15-minute",
          "30min": "30-minute"
        };
        const label = labelMap[presetKey] || labelMap[DEFAULT_PRESET];
        note.textContent = \`You are in \${label} review mode (\${totalQuestions} questions).\`;
      }

      function clearQuizContainer() {
        const container = document.getElementById('quiz-container');
        container.innerHTML = '';
        currentScore = 0;
        questionsAnswered = 0;
        totalQuestions = 0;
      }

      function loadProgress() {
        const progress = getProgress(TRACK_ID, MODULE_ID);

        if (progress.completed) {
          document.getElementById('progress-badge').style.display = 'block';
        }

        if (progress.attempts > 0) {
          document.getElementById('previous-score').style.display = 'block';
          document.getElementById('best-score-display').textContent = progress.bestScore;
          document.getElementById('attempts-display').textContent = progress.attempts;
        }

        return progress.lastPreset || DEFAULT_PRESET;
      }

      function saveProgress() {
        const progress = getProgress(TRACK_ID, MODULE_ID);
        const newBestScore = Math.max(progress.bestScore, currentScore);
        const isCompleted = totalQuestions > 0 && currentScore === totalQuestions;

        updateProgress(TRACK_ID, MODULE_ID, {
          attempts: progress.attempts + 1,
          bestScore: newBestScore,
          completed: isCompleted || progress.completed,
          lastVisited: Date.now(),
          lastPreset: activePreset
        });

        if (isCompleted) {
          document.getElementById('progress-badge').style.display = 'block';
        }
        document.getElementById('previous-score').style.display = 'block';
        document.getElementById('best-score-display').textContent = newBestScore;
        document.getElementById('attempts-display').textContent = progress.attempts + 1;
      }

      function generateStorageQuiz(presetKey = DEFAULT_PRESET) {
        const selectedQuestions = buildReviewSet(presetKey);
        const container = document.getElementById('quiz-container');
        totalQuestions = selectedQuestions.length;

        if (totalQuestions === 0) {
          container.innerHTML = '<div class="info-box">No questions available for this review mode.</div>';
          return;
        }

        selectedQuestions.forEach((q, index) => {
          const shuffledOptions = q.options.map((opt, idx) => ({ text: opt, isCorrect: idx === q.correctIndex }));
          const randomized = shuffleArray(shuffledOptions);

          const questionDiv = document.createElement('div');
          questionDiv.className = 'quiz-question';
          questionDiv.setAttribute('data-question', index + 1);

          const questionText = document.createElement('p');
          questionText.className = 'question-text';
          questionText.textContent = q.stem;
          questionDiv.appendChild(questionText);

          const optionsDiv = document.createElement('div');
          optionsDiv.className = 'quiz-options';

          randomized.forEach(option => {
            const btn = document.createElement('button');
            btn.className = 'quiz-option';
            btn.textContent = option.text;
            btn.dataset.correct = option.isCorrect;
            optionsDiv.appendChild(btn);
          });

          questionDiv.appendChild(optionsDiv);

          const feedback = document.createElement('div');
          feedback.className = 'quiz-feedback';
          questionDiv.appendChild(feedback);

          container.appendChild(questionDiv);
        });

        const scoreDiv = document.createElement('div');
        scoreDiv.className = 'quiz-score';
        scoreDiv.innerHTML = 'Score: <span id="score">0</span>/' + totalQuestions;
        container.appendChild(scoreDiv);

        document.querySelectorAll('.quiz-option').forEach(btn => {
          btn.addEventListener('click', handleStorageQuizAnswer);
        });
      }

      function handleStorageQuizAnswer() {
        const question = this.closest('.quiz-question');
        const options = question.querySelectorAll('.quiz-option');
        const feedback = question.querySelector('.quiz-feedback');
        const isCorrect = this.dataset.correct === 'true';

        options.forEach(opt => {
          opt.disabled = true;
          if (opt.dataset.correct === 'true') opt.classList.add('correct');
        });

        if (isCorrect) {
          this.classList.add('correct');
          feedback.textContent = '✓ Correct!';
          feedback.style.color = '#00ff88';
          currentScore++;
        } else {
          this.classList.add('incorrect');
          feedback.textContent = '✗ Incorrect.';
          feedback.style.color = '#ff4444';
        }
        questionsAnswered++;
        updateStorageScore();

        if (questionsAnswered === totalQuestions) {
          saveProgress();
        }
      }

      function updateStorageScore() {
        document.getElementById('score').textContent = currentScore;
      }

      function setReviewMode(presetKey, persist = true) {
        activePreset = REVIEW_PRESETS[presetKey] ? presetKey : DEFAULT_PRESET;
        clearQuizContainer();
        generateStorageQuiz(activePreset);
        setActiveButton(activePreset);
        updateReviewModeNote(activePreset);

        if (persist) {
          updateProgress(TRACK_ID, MODULE_ID, {
            lastPreset: activePreset,
            lastVisited: Date.now()
          });
        }
      }

      function bindReviewButtons() {
        document.querySelectorAll('.quiz-mode-controls button').forEach(btn => {
          btn.addEventListener('click', () => {
            setReviewMode(btn.dataset.review);
          });
        });
      }

      document.addEventListener('DOMContentLoaded', () => {
        const initialPreset = loadProgress();
        bindReviewButtons();
        setReviewMode(initialPreset, false);
      });

      // RAID Calculator
      document.getElementById('raid-calculate').addEventListener('click', () => {
        const level = parseInt(document.getElementById('raid-level').value);
        const driveCount = parseInt(document.getElementById('drive-count').value);
        const capacity = parseInt(document.getElementById('drive-capacity').value);
        const resultDiv = document.getElementById('raid-result');
        
        let usableCapacity, redundancy, minDrives, description;
        
        switch(level) {
          case 0:
            minDrives = 2;
            usableCapacity = driveCount * capacity;
            redundancy = 'None - Any drive failure = total data loss';
            description = 'RAID 0 stripes data across all drives for maximum performance and capacity.';
            break;
          case 1:
            minDrives = 2;
            usableCapacity = capacity;
            redundancy = \`Can survive \${driveCount - 1} drive failure(s)\`;
            description = 'RAID 1 mirrors data across drives. All drives contain identical data.';
            break;
          case 5:
            minDrives = 3;
            usableCapacity = (driveCount - 1) * capacity;
            redundancy = 'Can survive 1 drive failure';
            description = 'RAID 5 uses distributed parity. One drive worth of capacity for redundancy.';
            break;
          case 10:
            minDrives = 4;
            usableCapacity = (driveCount / 2) * capacity;
            redundancy = 'Can survive 1 drive failure per mirror pair';
            description = 'RAID 10 combines mirroring and striping for speed and redundancy.';
            break;
        }
        
        if (driveCount < minDrives) {
          resultDiv.innerHTML = \`<span style="color: #ff4444;">⚠ RAID \${level} requires at least \${minDrives} drives!</span>\`;
          return;
        }
        
        const rawTotal = driveCount * capacity;
        const efficiency = ((usableCapacity / rawTotal) * 100).toFixed(1);
        
        resultDiv.innerHTML = \`
          <strong>RAID \${level} Configuration:</strong><br>
          \${description}<br><br>
          <strong>Drives:</strong> \${driveCount} × \${capacity}GB = \${rawTotal}GB raw capacity<br>
          <strong style="color: #00d4ff; font-size: 1.2em;">Usable Capacity: \${usableCapacity}GB</strong><br>
          <strong>Storage Efficiency:</strong> \${efficiency}%<br>
          <strong>Redundancy:</strong> <span style="color: \${redundancy.includes('None') ? '#ff4444' : '#00ff88'}">\${redundancy}</span><br>
        \`;
      });
    </script>
    <script type="module" src="site.js"></script>
  </body>
</html>`} />
