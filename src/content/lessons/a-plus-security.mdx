---
title: Security & Compliance - A+ Study Guide
slug: a-plus-security
legacyUrl: /a-plus-security.html
difficulty: medium
estimatedMinutes: 15
---

import LegacyHtml from '../../components/LegacyHtml.astro';

<LegacyHtml html={`<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Security & Compliance - A+ Study Guide</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="site.css" />
    
  </head>
  <body>
    <a href="#main-content" class="skip-link">Skip to content</a>
    
    <header class="site-header">
      <div class="header-container">
        <div class="logo">
          <a href="/lessons/index">
            <span class="logo-bracket">[</span>
            <span class="logo-text">BEATTIE</span>
            <span class="logo-bracket">]</span>
            <span class="logo-subtitle">TECH</span>
          </a>
        </div>
        
        <button class="mobile-menu-toggle" aria-label="Toggle menu">
          <span></span>
          <span></span>
          <span></span>
        </button>
        
        <nav class="main-nav nav-links">
          <ul>
            <li><a href="/lessons/index">🏠 Home</a></li>
            <li><a href="/lessons/study-guides" class="active">📖 Study Guides</a></li>
            <li><a href="/lessons/learning-tracks">🎯 Learning Tracks</a></li>
            <li><a href="/lessons/resources">🔬 Study Hub</a></li>
            <li><a href="/lessons/cheat-sheets">⚡ Cheat Sheets</a></li>
          </ul>
        </nav>
      </div>
    </header>
    
    <div class="scanlines"></div>
    
    <main id="main-content" class="container">
      <nav class="breadcrumbs">
        <a href="/lessons/index">Home</a>
        <span class="separator">/</span>
        <a href="/lessons/study-guides">Study Guides</a>
        <span class="separator">/</span>
        <a href="/lessons/a-plus-guides">A+ Guides</a>
        <span class="separator">/</span>
        <span class="current">Security & Compliance</span>
      </nav>
      
      <h1>Security & Compliance</h1>
      <div class="subtitle">CompTIA A+ - Authentication, access control, malware, encryption, privacy laws</div>

      <div class="section">
        <h2>ELI5: Security</h2>
        <div class="info-box">
          Security is like locking your house. You want to keep good people in and bad people out. We use keys (passwords), cameras (monitoring), and alarms (alerts) to stay safe.
        </div>
      </div>

      <div class="section">
        <h2>Practical Exercises</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">Enable MFA</div><div class="description">Turn on MFA for a test account and explain the factors</div></div>
          <div class="command-item"><div class="command">Malware Match</div><div class="description">Match malware types to symptoms</div></div>
          <div class="command-item"><div class="command">Encrypt a USB</div><div class="description">Outline the steps for BitLocker or FileVault</div></div>
          <div class="command-item"><div class="command">Phishing Check</div><div class="description">Identify red flags in a sample email</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Authentication Methods</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">Something You Know</div><div class="description">Passwords, PINs, security questions</div></div>
          <div class="command-item"><div class="command">Something You Have</div><div class="description">USB key, smart card, phone (2FA)</div></div>
          <div class="command-item"><div class="command">Something You Are</div><div class="description">Fingerprint, face, iris scan (biometric)</div></div>
          <div class="command-item"><div class="command">Multi-Factor Auth (MFA)</div><div class="description">Combine 2+ methods (password + phone)</div></div>
          <div class="command-item"><div class="command">Single Sign-On (SSO)</div><div class="description">One login for multiple services</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Malware Types</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">Virus</div><div class="description">Attaches to files, spreads by execution</div></div>
          <div class="command-item"><div class="command">Worm</div><div class="description">Self-replicating, spreads via network</div></div>
          <div class="command-item"><div class="command">Trojan</div><div class="description">Disguised as legitimate software</div></div>
          <div class="command-item"><div class="command">Ransomware</div><div class="description">Encrypts data, demands payment</div></div>
          <div class="command-item"><div class="command">Spyware</div><div class="description">Monitors user activity secretly</div></div>
          <div class="command-item"><div class="command">Rootkit</div><div class="description">Hides malware deep in OS</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Malware Prevention</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">Antivirus Software</div><div class="description">Scans for known malware signatures</div></div>
          <div class="command-item"><div class="command">Windows Defender</div><div class="description">Built-in antivirus on Windows 10/11</div></div>
          <div class="command-item"><div class="command">Firewall</div><div class="description">Blocks suspicious incoming/outgoing traffic</div></div>
          <div class="command-item"><div class="command">Updates & Patches</div><div class="description">Fix known vulnerabilities</div></div>
          <div class="command-item"><div class="command">User Education</div><div class="description">Avoid suspicious downloads/links</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Encryption & Privacy Laws</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">BitLocker (Windows)</div><div class="description">Disk encryption for whole drive</div></div>
          <div class="command-item"><div class="command">FileVault (macOS)</div><div class="description">macOS disk encryption</div></div>
          <div class="command-item"><div class="command">GDPR</div><div class="description">EU privacy law (right to be forgotten)</div></div>
          <div class="command-item"><div class="command">HIPAA</div><div class="description">Healthcare privacy (US)</div></div>
          <div class="command-item"><div class="command">PCI-DSS</div><div class="description">Payment card industry security</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Mobile & IoT Security</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">Screen Lock</div><div class="description">PIN/biometric to prevent unauthorized access</div></div>
          <div class="command-item"><div class="command">Remote Wipe</div><div class="description">Erase data if device is lost</div></div>
          <div class="command-item"><div class="command">App Permissions</div><div class="description">Limit access to camera, mic, location</div></div>
          <div class="command-item"><div class="command">MDM</div><div class="description">Mobile Device Management for fleets</div></div>
          <div class="command-item"><div class="command">IoT Defaults</div><div class="description">Change default passwords and update firmware</div></div>
          <div class="command-item"><div class="command">Bluetooth Risks</div><div class="description">Disable when not in use</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Data Sanitization</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">Clear</div><div class="description">Logical removal (fast, less secure)</div></div>
          <div class="command-item"><div class="command">Purge</div><div class="description">Secure erase to prevent recovery</div></div>
          <div class="command-item"><div class="command">Destroy</div><div class="description">Physical destruction (shred, drill)</div></div>
          <div class="command-item"><div class="command">Crypto Erase</div><div class="description">Destroy encryption keys</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Network Security Basics</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">Firewall</div><div class="description">Allow/deny traffic by rules</div></div>
          <div class="command-item"><div class="command">IDS / IPS</div><div class="description">Detect and/or block suspicious activity</div></div>
          <div class="command-item"><div class="command">VPN</div><div class="description">Encrypted remote access tunnel</div></div>
          <div class="command-item"><div class="command">Least Privilege</div><div class="description">Only grant access needed to do the job</div></div>
          <div class="command-item"><div class="command">Patching</div><div class="description">Fix known vulnerabilities quickly</div></div>
          <div class="command-item"><div class="command">SIEM Basics</div><div class="description">Centralized log collection and alerts</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Access Control Models</h2>
        <div class="example-block">
          <div class="example-title">DAC vs MAC</div>
          <div class="example-code">DAC (Discretionary Access Control):
• Owner decides permissions
• Users can grant/revoke access
• Windows file permissions

MAC (Mandatory Access Control):
• System enforces access rules
• Users cannot change permissions
• Used in high-security environments</div>
        </div>
      </div>

      <div class="section">
        <h2>Physical Security</h2>
        <div class="command-grid">
          <div class="command-item"><div class="command">Locks & Cable Tethers</div><div class="description">Prevent device theft</div></div>
          <div class="command-item"><div class="command">Badges & Access Cards</div><div class="description">Control building access</div></div>
          <div class="command-item"><div class="command">Mantraps</div><div class="description">Single‑person entry control</div></div>
          <div class="command-item"><div class="command">Cameras & Logs</div><div class="description">Monitor and record access</div></div>
        </div>
      </div>

      <div class="section">
        <h2>Exam Critical Facts</h2>
        <div class="example-block">
          <div class="example-title">Must Know</div>
          <div class="example-code">• Always disable default accounts
• Enforce strong password policies (12+ chars, mixed case)
• Never share admin passwords
• Use encryption for sensitive data
• Keep antivirus up to date
• Educate users about phishing
• Disable unnecessary services
• Audit and log all access attempts</div>
        </div>
      </div>

      <div class="section">
        <h2>Interactive Quiz</h2>
        <div id="progress-badge" style="display: none; text-align: center; margin-bottom: 15px;">
          <span style="background: linear-gradient(135deg, #00ff88, #00d4ff); color: #0a0e27; padding: 8px 20px; border-radius: 20px; font-weight: 700; font-size: 0.95em;">
            ✓ COMPLETED
          </span>
        </div>
        <div id="previous-score" style="display: none; text-align: center; margin-bottom: 15px; color: #00d4ff;">
          Previous Best: <strong id="best-score-display">0</strong> | Attempts: <strong id="attempts-display">0</strong>
        </div>
        <div class="info-box" style="margin-bottom: 20px;">
          <strong>Dynamic Quiz:</strong> Questions randomized from an expanded pool. Refresh for new questions!
        </div>
        <div class="quiz-mode-controls">
          <label>Review length:</label>
          <button data-review="5min">5 min</button>
          <button data-review="15min">15 min</button>
          <button data-review="30min">30 min</button>
        </div>
        <div id="review-mode-note" class="review-mode-note"></div>
        <div id="quiz-container">
          {/* Questions generated dynamically */}
        </div>
      </div>

      <div class="section">
        <h2>Mini-Lab: Password Strength Checker</h2>
        <div class="lab-container">
          <div class="info-box">
            <strong>Objective:</strong> Test password strength against security best practices
          </div>
          <div class="lab-challenge">
            <div class="psu-input-row">
              <label>Enter Password:</label>
              <input type="text" id="password-input" placeholder="Type a password to test">
            </div>
            <div class="lab-input-group">
              <button id="check-password">Check Strength</button>
            </div>
            <div id="password-result" class="psu-result"></div>
          </div>
        </div>
      </div>

      <div class="section">
        <h2>Exam Critical Facts</h2>
        <div class="example-block">
          <div class="example-title">Must Know</div>
          <div class="example-code">• Always disable default accounts
• Enforce strong password policies (12+ chars, mixed case)
• Never share admin passwords
• Use encryption for sensitive data
• Keep antivirus up to date
• Educate users about phishing
• Disable unnecessary services
• Audit and log all access attempts</div>
        </div>
      </div>
    </div>

    

    <script type="module">
      import { aPlusSecurityQuestions } from "./data/a-plus-security-questions.js";
      import { getProgress, updateProgress } from "./js/progressStore.js";

      const TRACK_ID = "pc-professional";
      const MODULE_ID = "a-plus-security";

      const REVIEW_PRESETS = {
        "5min": 6,
        "15min": 15,
        "30min": 30
      };

      const DEFAULT_PRESET = "5min";

      let currentScore = 0;
      let questionsAnswered = 0;
      let totalQuestions = 0;
      let activePreset = DEFAULT_PRESET;

      function shuffleArray(array) {
        const shuffled = [...array];
        for (let i = shuffled.length - 1; i > 0; i--) {
          const j = Math.floor(Math.random() * (i + 1));
          [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
        }
        return shuffled;
      }

      function buildReviewSet(presetKey) {
        const numQuestions = REVIEW_PRESETS[presetKey] || REVIEW_PRESETS[DEFAULT_PRESET];

        const easy = aPlusSecurityQuestions.filter(q => q.difficulty === "easy");
        const med = aPlusSecurityQuestions.filter(q => q.difficulty === "medium");
        const hard = aPlusSecurityQuestions.filter(q => q.difficulty === "hard");

        let pool;
        if (presetKey === "5min") {
          pool = [...easy, ...med];
        } else if (presetKey === "15min") {
          pool = [...easy, ...med, ...hard];
        } else {
          pool = [...med, ...hard];
        }

        if (pool.length === 0) {
          pool = [...aPlusSecurityQuestions];
        }

        const shuffled = shuffleArray(pool);
        return shuffled.slice(0, Math.min(numQuestions, shuffled.length));
      }

      function setActiveButton(presetKey) {
        document.querySelectorAll('.quiz-mode-controls button').forEach(btn => {
          btn.classList.toggle('active', btn.dataset.review === presetKey);
        });
      }

      function updateReviewModeNote(presetKey) {
        const note = document.getElementById('review-mode-note');
        const labelMap = {
          "5min": "5-minute",
          "15min": "15-minute",
          "30min": "30-minute"
        };
        const label = labelMap[presetKey] || labelMap[DEFAULT_PRESET];
        note.textContent = \`You are in \${label} review mode (\${totalQuestions} questions).\`;
      }

      function clearQuizContainer() {
        const container = document.getElementById('quiz-container');
        container.innerHTML = '';
        currentScore = 0;
        questionsAnswered = 0;
        totalQuestions = 0;
      }

      function loadProgress() {
        const progress = getProgress(TRACK_ID, MODULE_ID);

        if (progress.completed) {
          document.getElementById('progress-badge').style.display = 'block';
        }

        if (progress.attempts > 0) {
          document.getElementById('previous-score').style.display = 'block';
          document.getElementById('best-score-display').textContent = progress.bestScore;
          document.getElementById('attempts-display').textContent = progress.attempts;
        }

        return progress.lastPreset || DEFAULT_PRESET;
      }

      function saveProgress() {
        const progress = getProgress(TRACK_ID, MODULE_ID);
        const newBestScore = Math.max(progress.bestScore, currentScore);
        const isCompleted = totalQuestions > 0 && currentScore === totalQuestions;

        updateProgress(TRACK_ID, MODULE_ID, {
          attempts: progress.attempts + 1,
          bestScore: newBestScore,
          completed: isCompleted || progress.completed,
          lastVisited: Date.now(),
          lastPreset: activePreset
        });

        if (isCompleted) {
          document.getElementById('progress-badge').style.display = 'block';
        }
        document.getElementById('previous-score').style.display = 'block';
        document.getElementById('best-score-display').textContent = newBestScore;
        document.getElementById('attempts-display').textContent = progress.attempts + 1;
      }

      function generateSecurityQuiz(presetKey = DEFAULT_PRESET) {
        const selectedQuestions = buildReviewSet(presetKey);
        const container = document.getElementById('quiz-container');
        totalQuestions = selectedQuestions.length;

        if (totalQuestions === 0) {
          container.innerHTML = '<div class="info-box">No questions available for this review mode.</div>';
          return;
        }

        selectedQuestions.forEach((q, index) => {
          const shuffledOptions = q.options.map((opt, idx) => ({ text: opt, isCorrect: idx === q.correctIndex }));
          const randomized = shuffleArray(shuffledOptions);

          const questionDiv = document.createElement('div');
          questionDiv.className = 'quiz-question';
          questionDiv.setAttribute('data-question', index + 1);

          const questionText = document.createElement('p');
          questionText.className = 'question-text';
          questionText.textContent = q.stem;
          questionDiv.appendChild(questionText);

          const optionsDiv = document.createElement('div');
          optionsDiv.className = 'quiz-options';

          randomized.forEach(option => {
            const btn = document.createElement('button');
            btn.className = 'quiz-option';
            btn.textContent = option.text;
            btn.dataset.correct = option.isCorrect;
            optionsDiv.appendChild(btn);
          });

          questionDiv.appendChild(optionsDiv);

          const feedback = document.createElement('div');
          feedback.className = 'quiz-feedback';
          questionDiv.appendChild(feedback);

          container.appendChild(questionDiv);
        });

        const scoreDiv = document.createElement('div');
        scoreDiv.className = 'quiz-score';
        scoreDiv.innerHTML = 'Score: <span id="score">0</span>/' + totalQuestions;
        container.appendChild(scoreDiv);

        document.querySelectorAll('.quiz-option').forEach(btn => {
          btn.addEventListener('click', handleSecurityQuizAnswer);
        });
      }

      function handleSecurityQuizAnswer() {
        const question = this.closest('.quiz-question');
        const options = question.querySelectorAll('.quiz-option');
        const feedback = question.querySelector('.quiz-feedback');
        const isCorrect = this.dataset.correct === 'true';

        options.forEach(opt => {
          opt.disabled = true;
          if (opt.dataset.correct === 'true') opt.classList.add('correct');
        });

        if (isCorrect) {
          this.classList.add('correct');
          feedback.textContent = '✓ Correct!';
          feedback.style.color = '#00ff88';
          currentScore++;
        } else {
          this.classList.add('incorrect');
          feedback.textContent = '✗ Incorrect.';
          feedback.style.color = '#ff4444';
        }

        questionsAnswered++;
        updateSecurityScore();

        if (questionsAnswered === totalQuestions) {
          saveProgress();
        }
      }

      function updateSecurityScore() {
        document.getElementById('score').textContent = currentScore;
      }

      function setReviewMode(presetKey, persist = true) {
        activePreset = REVIEW_PRESETS[presetKey] ? presetKey : DEFAULT_PRESET;
        clearQuizContainer();
        generateSecurityQuiz(activePreset);
        setActiveButton(activePreset);
        updateReviewModeNote(activePreset);

        if (persist) {
          updateProgress(TRACK_ID, MODULE_ID, {
            lastPreset: activePreset,
            lastVisited: Date.now()
          });
        }
      }

      function bindReviewButtons() {
        document.querySelectorAll('.quiz-mode-controls button').forEach(btn => {
          btn.addEventListener('click', () => {
            setReviewMode(btn.dataset.review);
          });
        });
      }

      document.addEventListener('DOMContentLoaded', () => {
        const initialPreset = loadProgress();
        bindReviewButtons();
        setReviewMode(initialPreset, false);
      });

      document.getElementById('check-password').addEventListener('click', () => {
        const password = document.getElementById('password-input').value;
        const result = document.getElementById('password-result');
        
        if (!password) {
          result.innerHTML = '<span style="color: #ff4444;">⚠ Please enter a password</span>';
          return;
        }
        
        let score = 0;
        const checks = {
          length: password.length >= 12,
          upper: /[A-Z]/.test(password),
          lower: /[a-z]/.test(password),
          digit: /[0-9]/.test(password),
          special: /[^A-Za-z0-9]/.test(password),
          notCommon: !['password', '12345678', 'qwerty', 'letmein'].includes(password.toLowerCase())
        };
        
        score = Object.values(checks).filter(v => v).length;
        const percentage = (score / 6) * 100;
        
        let strength, color, barColor;
        if (percentage < 40) { strength = 'Weak'; color = '#ff4444'; barColor = '#ff4444'; }
        else if (percentage < 70) { strength = 'Moderate'; color = '#ffaa00'; barColor = '#ffaa00'; }
        else if (percentage < 100) { strength = 'Strong'; color = '#00d4ff'; barColor = '#00d4ff'; }
        else { strength = 'Excellent'; color = '#00ff88'; barColor = '#00ff88'; }
        
        result.innerHTML = \`
          <strong style="color: \${color}; font-size: 1.3em;">Password Strength: \${strength}</strong><br>
          <div class="strength-bar" style="width: \${percentage}%; background: \${barColor};"></div><br>
          <strong>Security Checks:</strong><br>
          \${checks.length ? '✓' : '✗'} At least 12 characters<br>
          \${checks.upper ? '✓' : '✗'} Contains uppercase letter<br>
          \${checks.lower ? '✓' : '✗'} Contains lowercase letter<br>
          \${checks.digit ? '✓' : '✗'} Contains digit<br>
          \${checks.special ? '✓' : '✗'} Contains special character (!@#$%)<br>
          \${checks.notCommon ? '✓' : '✗'} Not a common password<br><br>
          <strong>Tips:</strong> \${percentage < 100 ? 'Add missing elements to improve strength' : 'Great password! Consider using a password manager.'}
        \`;
      });
    </script>
    <script type="module" src="site.js"></script>
  </body>
</html>`} />
